<h1>Edit Student Page</h1>
<h3>Your current major is: <%= @student.major %>.

<%= form_with model: @student do |form| %>
    <%= form.select(:major, options_for_select(["Mime", "Circus Clown"])) %>
    <%= form.submit %>
<% end %>
<%= link_to "Home", root_path %>