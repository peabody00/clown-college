<h1>Create New Courses!</h1>
<% if @course.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>
    <ul>
      <% @course.errors.full_messages.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with model: [@teacher, @course], local: true do |form| %>
    Course Name: <%= form.text_field :name %><br>
    Course Description: <%= form.text_area :description %><br>
    <% form.hidden_field :teacher_id, value: @teacher.id %> 
    <%= form.submit %>
<% end %>
<%= link_to "Home", root_path %>